{
  "name": "@c4ml/spike-language-langium",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "generate": "langium generate",
    "build": "pnpm run generate && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run generate && vitest run --exclude 'dist/**'"
  },
  "dependencies": {
    "langium": "4.3.1"
  },
  "devDependencies": {
    "langium-cli": "4.3.0"
  }
}
