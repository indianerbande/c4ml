# Frontend applications

The Node.js CLI and desktop editor live here. The editor uses the accepted
Angular 22 and Monaco 0.56.0 production stack. The CLI and editor must use the
portable compiler core and its adapter contracts; frontend code must not become
a second source of C4ML semantics.
