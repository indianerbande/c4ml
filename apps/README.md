# Frontend applications

The Node.js CLI and browser editor will live here. Phase 0 intentionally does
not select an editor UI framework or code-editor component. Both frontends must
use the portable compiler core and its adapter contracts.
